<form method="post" action="{{ path('app_lieux_delete', {'id': lieux.id}) }}" onsubmit="return confirm('Impossible');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lieux.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
