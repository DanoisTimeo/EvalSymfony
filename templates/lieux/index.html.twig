{% extends 'base.html.twig' %}

{% block title %}Lieux index{% endblock %}

{% block body %}
    <h1>Lieux index</h1>

    <a href="{{ path('app_releves_index') }}" class="btn btn-info">retour au relevés</a>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lieux in lieuxes %}
            <tr>
                <td>{{ lieux.nom }}</td>
                <td style="display: flex">
                    {{ include('lieux/_delete_form.html.twig') }}
                    <a href="{{ path('app_lieux_edit', {'id': lieux.id}) }}" class="btn btn-info" style="width: 80px; font-size: 13px">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a href="{{ path('app_lieux_new') }}" class="btn btn-success">Ajouter</a>

{% endblock %}
